<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVM_SQLSERVER.Views.Main.DashboardPage"
             Title="Dashboard">
    <VerticalStackLayout>
        <Button Text="Ver Todos los Usuarios" 
                Command="{Binding ViewAllUsersCommand}"/>

        <Button Text="Crear Nuevo Usuario" 
                Command="{Binding GoToCreateUserCommand}"/>

        <Button Text="Buscar Usuario" 
                Command="{Binding GoToSearchUserCommand}"/>

        <Button Text="Cerrar Sesión" 
                Command="{Binding LogoutCommand}"
                Style="{StaticResource DangerButton}"/>
    </VerticalStackLayout>
</ContentPage>